services:
  - type: web
    name: blog-splitter-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --workers 4 --bind 0.0.0.0:$PORT --timeout 120 app:app"
